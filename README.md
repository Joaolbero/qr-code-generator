# QR Code Generator / Gerador de QR Code

**EN:** Simple CLI + Web API to generate QR Codes (text/URL, Wi-Fi, vCard) using `segno`.  
**PT-BR:** CLI + API Web para gerar QR Codes (texto/URL, Wi-Fi, vCard) com `segno`.

## Quickstart
1) Install Python 3.10+ and pip  
2) Install deps:
```bash
pip install -r requirements.txt